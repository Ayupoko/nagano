<h3>商品一覧（全<%= Item.count %>件）</h3>


  <% @item.each do |item| %>
    <%= link_to item_path(item) do %>
    <%= attachment_image_tag item, :image, :size => '100x100' %>
    <% end %></br>
    <%= item.name %></br>
    ￥  <%= (1.1*(item.price) ).round %></br></br>
  <% end %>
